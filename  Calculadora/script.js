import prompt from 'prompt-sync';
// receber 
function receberExpressao() {
    let expr = prompt("Digite a sua f(x): ");
    console.log(expr);
    return expr;
}

function parseExpressao(expr) {
    
}